// Single-file build: no bundled image bytes. Every image comes from the
// window.LENS_ASSETS URLs configured in the host page; missing entries fall
// back to the typographic lockups / gradient background.
export const policeImg = '';
export const centerImg = '';
export const badgeImg = '';
export const bgImg = '';
